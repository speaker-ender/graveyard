import styled from "styled-components";

export const StyledFunction = styled.div`
    background: linear-gradient(45deg, var(--tertiary), var(--background));
    color: var(--text);
    background-blend-mode: invert;
    border-radius: 25px;
    padding: 25px;
    margin-bottom: 25px;
`;