import { PanelStyles } from "global/panel.styles";
import styled from "styled-components";

export const StyledSwapperInfo = styled.div`
    ${PanelStyles}
    background-color: var(--background-invert);
    color: var(--text-invert);
`